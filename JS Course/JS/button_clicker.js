function loginBtn(e) {
    // when we click the button 
    //change button text from login to logout 
    e.innerText = "Log out";
}


// remove the button when we click it
function removeDef(e){
    e.remove();
}

// when click the button alert message will appear
function alert(){
    alert('ninja was liked');
}
